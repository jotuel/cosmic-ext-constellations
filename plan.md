1. **Analyze existing localization issues**: The current codebase uses hardcoded English strings in multiple areas instead of the available localization system (`crate::fl!()`). Specifically, many strings wrapped in `button::text("")` or `text::body("")` in `src/settings/user.rs` are hardcoded.
2. **Add missing keys to fluent files**: I will define all these localized text items in the English localization file (`res/i18n/en/cosmic_ext_constellations.ftl`).
3. **Refactor `src/settings/user.rs`**: I will replace all hardcoded text strings like "Loading keywords...", "Loading profile...", "No Avatar", "Make changes to save", "Fill in all fields to change password", "New passwords do not match", "Loading devices...", "✅ Verified", "❌ Unverified", "Delete Device", "Rename Device", "Current", "Loading cross-signing status...", "Loading linked accounts...", "Loading ignored users...", "Waiting for other device to accept...", "Do these emojis match on both devices?", "Verification successful!", and "Verification cancelled or failed." with `crate::fl!()` calls.
4. **Complete Pre-Commit Steps**: Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
