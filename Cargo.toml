[package]
name = "test_rekey"
version = "0.1.0"
edition = "2021"

[dependencies]
matrix-sdk-sqlite = "0.16.0"
rusqlite = { version = "0.37", features = ["bundled"] }
tokio = { version = "1", features = ["full"] }
